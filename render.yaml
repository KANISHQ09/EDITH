services:
  # ─────────────────────────────────────────────────────────────────────────
  # 1. EDITH REST API Service
  # ─────────────────────────────────────────────────────────────────────────
  - type: web
    name: edith-api
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install --include=dev && npm run build:backend
    startCommand: npm run migrate && npm run start:api
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MESSAGE_BROKER
        value: redis
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: DATABASE_URL
        fromDatabase:
          name: edith-postgres
          property: connectionString
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: AGORA_APP_ID
        value: aec8fcc91dd640968f8c4935fc1fdbd7
      - key: AGORA_APP_CERTIFICATE
        value: d2bb47693dab4ba891078b78fd3d6f07

  # ─────────────────────────────────────────────────────────────────────────
  # 2. EDITH WebSocket Gateway (Real-Time Live Streaming)
  # ─────────────────────────────────────────────────────────────────────────
  - type: web
    name: edith-websocket
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install --include=dev && npm run build:backend
    startCommand: npm run start:wsg
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MESSAGE_BROKER
        value: redis
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        fromService:
          type: web
          name: edith-api
          envVarKey: JWT_SECRET

databases:
  # ─────────────────────────────────────────────────────────────────────────
  # Managed PostgreSQL Database
  # ─────────────────────────────────────────────────────────────────────────
  - name: edith-postgres
    databaseName: vaic
    user: vaic
    plan: free
    region: oregon
